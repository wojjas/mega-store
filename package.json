{
  "name": "mega-store",
  "version": "0.1.0",
  "private": true,
  "devDependencies": {
    "react-scripts": "0.7.0",
    "foreman": "^2.0.0"
  },
  "scripts": {
    "dev": "nf start --procfile Procfile.dev",
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  }
}
